
server:
  port: 8888

# Redis数据库索引（默认为0）
spring:
  redis:
    port: 6379
    host: localhost
    password:
    jedis:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0

